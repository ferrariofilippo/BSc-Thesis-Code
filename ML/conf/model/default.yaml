################################################################################
# Neural Network
input_dim: 58
hidden_1_dim: 32
hidden_2_dim: 16
hidden_3_dim: 16
output_dim: 1

# relu | leaky_relu | gelu | tanh
hidden_activation: relu

# Log-tolerance regression requires a linear output. The prediction is converted
# back with 10**log_tolerance, which guarantees a positive tolerance.
output_activation: none

################################################################################
# Tree
max_depth: 8
min_samples_split: 8
min_samples_leaf: 8

################################################################################
# Random Forest
n_estimators: 100
max_features: 8
n_jobs: -1

################################################################################
# Gaussian Process
constant_value: 1.0
constant_bounds: [1e-3, 1e3]

length_scale: 1.0
length_scale_bounds: [5e-1, 1e2]

noise_level: 1.0
noise_level_bounds: [1e-5, 1e1]

n_restarts_optimizer: 10

################################################################################
# Response Surface Method

degree: 2
interaction_only: false
alpha: 1.0   
